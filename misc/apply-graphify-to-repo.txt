TASK: Enable graphify (the knowledge graph) IN THIS REPO. This is a per-repo, opt-in
decision and the ONLY graphify step that is per-repo.

SCOPE - what is already done for you (do NOT redo it here):
Everything GLOBAL is wired machine-wide by misc/update-python-and-context-tools.ps1:
  - rtk + headroom run always-on in every repo;
  - the /graphify skill is installed in each harness;
  - graphify's CONDITIONAL steering is in each harness' GLOBAL config (a
    "## graphify graph usage" block in ~/.claude/CLAUDE.md and ~/.codex/AGENTS.md, plus a
    PreToolUse grep hook in ~/.claude/settings.json). It is guarded on
    graphify-out/graph.json and stays INERT until this repo builds a graph.
So do NOT edit any global harness instructions, hooks, or env vars, and do NOT configure
rtk/headroom at all. This prompt only measures the layer and builds the graph.

VERSION POLICY: reference tools by COMMAND NAME only, never pin a version. Do not install
or update rtk/headroom/graphify or the harness binaries - the installer keeps them current.
If graphify is not on PATH, STOP and tell me to run misc/update-python-and-context-tools.ps1.

DO THIS:
1. MEASURE the layer per support/context-tooling.md. LOC ratio, excluding
   generated/transient files: KNOWLEDGE = .scaffold/ + docs/*.md ; CODE = src/ (*.cs,
   *.razor, *.ts, *.tsx, *.xaml). .instructions/ and HANDOFF.md are excluded from both the
   measurement and the graph corpus (generic scaffold payload / transient resume state).
   Report KNOWLEDGE, CODE, and the ratio.
2. RECOMMEND structure-only (AST, no model spend) vs full (AST + semantic LLM) using the
   decision table in support/context-tooling.md, with a one-line reason. Wait for my OK.
3. ON APPROVAL, initialize exactly per support/context-tooling.md:
   - write .graphifyignore (bin/obj/dist/node_modules/test output/EF migrations/rendered
     HTML/etc., plus .instructions/ and HANDOFF.md per the corpus rule above);
   - set the graphify-out/ split in the repo-root .gitignore. COMMIT graph.json,
     GRAPH_REPORT.md, graph.html. IGNORE manifest.json, cost.json, cache/, wiki/, obsidian/,
     .graphify_*. The wiki is gitignored BY DEFAULT - it is regenerable from graph.json via
     `graphify export wiki` (no model cost). Commit graphify-out/wiki/ only if I explicitly
     ask.
   - build from the repo root:
       full layer:           `graphify . --wiki` - builds the graph AND the agent-crawlable
                             wiki (graphify-out/wiki/index.md + one article per community + god
                             nodes), which the global steering points agents to. Always also
                             produces graph.json, GRAPH_REPORT.md, graph.html. No API key is
                             needed for the semantic/LLM extraction when you run this inside a
                             harness session (Claude Code / Claude VS Code) - the host session
                             makes the LLM calls. (Headless/CI only: set GEMINI_API_KEY or
                             GOOGLE_API_KEY.)
       structure-only layer: let .graphifyignore additionally exclude docs/ and .scaffold/,
                             then `graphify . --no-cluster` (no communities, so a wiki adds
                             little; skip --wiki). Refresh with `graphify update .`.
     Confirm graphify-out/graph.json (and, for the full layer, graphify-out/wiki/index.md)
     exists - a global install or harness registration is NOT a built graph.
4. KEEP IT FRESH (recommended): run `graphify hook install` to add the post-commit and
   post-checkout git hooks so code changes auto-update the graph. They rebuild the CODE (AST)
   layer in the background after every commit by any tool or human - no API cost, no extra
   commit. The hooks live in .git/hooks/ (untracked), so they do NOT leak into scaffolded
   apps and are safe even in a template repo. CAVEAT: this refreshes the code (AST) layer
   ONLY. It does NOT regenerate the wiki or the semantic/doc layer (.scaffold/, docs).
   To refresh the wiki cheaply from the updated graph run
   `graphify export wiki` (renders from graph.json, no model cost); for the semantic/doc layer
   rerun a full `graphify . --wiki` at phase boundaries. For a structure-only repo the hook
   alone keeps the graph current.

PER-REPO HARNESS WIRING - read carefully:
- The global conditional steering already routes codebase questions to the graph in ANY repo
  once graphify-out/graph.json exists, for every harness. You usually do NOT need
  `graphify claude/codex/copilot install --project`.
- NEVER commit graphify wiring into a TEMPLATE / scaffold repo - it would leak into every app
  created from it. (This repository is such a template.) In an ordinary application repo,
  `graphify <h> install --project` is optional and fine, but it writes TRACKED files, so only
  do it if I explicitly ask.
- If this repo ALREADY has graphify wiring committed in CLAUDE.md / AGENTS.md /
  .github/copilot-instructions.md / .claude/settings.json / .codex/hooks.json, REMOVE it by
  running misc/strip-graphify-repo-wiring.ps1 from the repo root. It strips per-repo wiring
  while leaving the global steering and the built graph intact.

REPORT: the measured ratio, the chosen layer and why, the files written, confirmation that
graphify-out/graph.json exists, whether the auto-update hook was installed, and whether any
leaked per-repo wiring was stripped.
